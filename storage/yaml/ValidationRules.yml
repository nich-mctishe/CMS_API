Client:
    ValidationRules:
        - {name: name, criteria: required}
Image: #these criteria might have to change as they are porbably server generated.
    ValidationRules:
        - {name: mimeType, criteria: image}
Project:
    ValidationRules:
        - {name: title, criteria: required|string}
        - {name: description, criteria: required|string}
        - {name: moreInfo, criteria: string}
        - {name: skillTags, criteria: array}
Projlet:
    ValidationRules:
        - {name: title, criteria: required}
        - {name: description, criteria: required}
        - {name: skillTags, criteria: array}
Skill:
    ValidationRules:
        - {name: name, criteria: required}
        - {name: desc, criteria: ''}
        - {name: categoryId, criteria: required|integer}
SkillCategory:
    ValidationRules:
        - {name: name, criteria: required}
SkillTag:
    ValidationRules:
        - {name: skillId, criteria: required|numeric}
WorkExperience:
    ValidationRules:
        - {name: name, criteria: required}
        - {name: description, criteria: required}